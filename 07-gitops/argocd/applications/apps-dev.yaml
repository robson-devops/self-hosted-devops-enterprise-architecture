apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-dev
  namespace: argocd
spec:
  project: platform-project

  source:
    repoURL: https://github.com/robson-devops/self-hosted-devops-enterprise-architecture.git
    targetRevision: main
    path: 05-kubernetes/apps

  destination:
    server: https://kubernetes.default.svc
    namespace: apps

  syncPolicy:
    automated:
      prune: true
      selfHeal: true