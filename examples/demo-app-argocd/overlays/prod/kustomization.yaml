apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -prod

namespace: prod

commonLabels:
  environment: prod

images:
  - name: nginx
    newTag: 1.25.3