apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -staging

namespace: staging

commonLabels:
  environment: staging

images:
  - name: nginx
    newTag: 1.25.2